# 01 Asset Driven Scheduling

- Triggering DAGs based on dataset/asset updates (@asset / datasets) rather than rigid cron
- Using cross-DAG dependencies driven by external data changes instead of explicit sensors
