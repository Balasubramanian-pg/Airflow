# 04 Production Architecture and Testing

- Role of core components (Scheduler, Metadata Database, Webserver, Workers/Executors)
- Writing unit tests for DAG structure and task logic using pytest
- Avoiding anti-patterns like heavy computation in top-level DAG parsing code
